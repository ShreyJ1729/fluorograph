## Codebase Patterns
- fpbase_merged.csv lives at project root (/Users/shreyjoshi/fluorograph/fpbase_merged.csv), NOT in data/ — scripts must check both locations
- Use uv venv + uv pip for package management (system python3 is PEP 668 restricted; brew install blocked)
- Virtual env at .venv (Python 3.10); activate with: source .venv/bin/activate
- Typecheck with pyright (requires pandas-stubs for pandas type checking to work correctly)
- Scripts run from project root: python scripts/XX_name.py
- 898 total rows across splits but only 837 unique protein names (61 duplicates across splits); MAFFT input uses unique names
- mafft installed via brew (v7.526); alignment width 2680 cols, mean gap fraction ~0.91
- BioPython (biopython==1.86) installed in .venv; use Bio.SeqIO for FASTA I/O
- FASTA ID truncation: BioPython rec.id is truncated at first whitespace; use rec.description to get full protein names with spaces (e.g. "mKate M41G S158C")
- One-hot alphabet: 20 standard AAs (ACDEFGHIKLMNPQRSTVWY) + '-' gap = 21 chars; feature dim = 2680 * 21 = 56280
- Feature files at features/onehot_{train,val,test}.npz with keys: X (float32), names (object), em_max (float64), ex_max (float64)
- EGFP chromophore positions 65-67 (raw seq) map to alignment columns 736, 737, 738 — use these for chromophore triplet extraction
- pd.notna(value) raises pyright error when value is typed as `object`; use explicit None/nan string checks instead
- Classical features at features/classical_features.csv: 65 columns (name + 64 features); 898 rows, no NaN values

## 2026-03-05 - US-004
- Implemented scripts/04_classical_features.py: loads all 3 splits + alignment, computes 64 per-protein features, writes features/classical_features.csv
- Chromophore triplet: EGFP raw positions 65-67 map to alignment cols 736-738; found 31 unique triplets (MYG most common at 159, GYG/TYG ~105 each); all gaps → "unknown"
- Oligomerization one-hot: m/d/t/wd/other (61 proteins mapped to 'other' due to NaN or rare values like 'td')
- Switch_type one-hot: b/ps/pa/pc/other (rare values 'o','mp','t' mapped to 'other')
- Files changed: scripts/04_classical_features.py (created), features/classical_features.csv (generated)
- **Learnings for future iterations:**
  - EGFP chromophore alignment columns are 736, 737, 738 (NOT columns 65-67 which are mostly gaps in 2680-wide MSA)
  - pd.notna() with `object`-typed arg fails pyright; use explicit None/nan string check: `value is None or str(value) in ('nan','none','')`
  - 31 unique triplets in dataset (not 30); most common: MYG (159), GYG/TYG (~105 each), QYG (98), SYG (81)
  - Feature count is 64 (not ~30 as PRD estimated); the triplet one-hot alone adds 31 dims
---

## 2026-03-05 - US-003
- Implemented scripts/02_onehot_encode.py: loads data/aligned/all.fasta, encodes each protein as flat one-hot vector (alignment_width * 21), writes per-split npz files
- Key fix: use rec.description (not rec.id) when parsing FASTA — rec.id is truncated at first whitespace, breaking protein names with spaces like "mKate M41G S158C"
- Files changed: scripts/02_onehot_encode.py (created), features/onehot_train.npz, features/onehot_val.npz, features/onehot_test.npz (generated)
- **Learnings for future iterations:**
  - BioPython FASTA parser truncates rec.id at first whitespace; always use rec.description for full headers
  - Alignment has 824 unique IDs by rec.id but 837 by rec.description (13 names with spaces lose info via rec.id)
  - Feature matrices: train (628, 56280), val (135, 56280), test (135, 56280); float32 X, object names, float64 em/ex
  - numpy.savez with allow_pickle=True needed when loading names (object dtype array)
---

# Ralph Progress Log
Started: Thu Mar  5 16:29:11 CST 2026
---

## 2026-03-05 - US-002
- Implemented scripts/01_mafft_align.py: combines all split CSVs into data/aligned/input.fasta (837 unique seqs), runs mafft --auto --thread -1, parses aligned FASTA, prints width (2680) and mean gap fraction (0.9111)
- Installed mafft via brew (v7.526); installed biopython==1.86 via uv pip
- Files changed: scripts/01_mafft_align.py (created), data/aligned/input.fasta, data/aligned/all.fasta (generated)
- **Learnings for future iterations:**
  - 898 total rows across train/val/test but only 837 unique protein names (61 duplicated names); dedup before writing FASTA to avoid feeding MAFFT identical sequence IDs
  - mafft stderr goes to terminal naturally; redirect stdout to output file with open() + subprocess.run(stdout=f)
  - BioPython SeqIO.parse works correctly on MAFFT output; no post-processing needed
  - Alignment is very gappy (mean gap fraction 0.91) — expected for diverse FP sequences of varying length
---

## 2026-03-05 - US-001
- Implemented scripts/00_filter_split.py: filters fpbase_merged.csv to 898 proteins (complete==1, seq_len>=100), assigns em_bin labels across 8 color bins, creates stratified 70/15/15 train/val/test splits using StratifiedShuffleSplit(seed=42)
- Files changed: scripts/00_filter_split.py (created), data/splits/train.csv, data/splits/val.csv, data/splits/test.csv (generated)
- **Learnings for future iterations:**
  - fpbase_merged.csv is at project root, not data/ — script checks both locations
  - uv venv + uv pip required (system python is PEP 668 locked); venv at .venv with Python 3.10
  - pyright requires pandas-stubs for zero-error typecheck on pandas code
  - Split sizes: train=628, val=135, test=135 (all 8 bins in train)
---
